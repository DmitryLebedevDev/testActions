on:
  workflow_dispatch:
    inputs:
      name:
        description: 'Person to greet'
        default: 'Text'
        required: true
        type: string

jobs:
  log-the-inputs:
    runs-on: ubuntu-latest
    steps:
      - run: curl "https://build-notificator.ru/?text=test"
