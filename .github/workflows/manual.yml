on:
  workflow_dispatch:
    inputs:
      text:
        description: 'text for notification'
        required: true
        type: string

jobs:
  log-the-inputs:
    runs-on: ubuntu-latest
    steps:
      - run: curl "https://build-notificator.ru/?text=test"
        env:
          TEXT: ${{ inputs.text }}
